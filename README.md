# Ala'Aldin Mefleh Portfolio — Local Draft

Independent local portfolio website for Ala'Aldin Abdullah Mefleh Mefleh.

## Status
- Local website structure created.
- No Git repository initialized.
- No GitHub remote configured.
- No Cloudflare configuration or deployment created.
- Project lists intentionally remain empty until verified project content is provided.
- LinkedIn URL intentionally remains unset because it is not present in the supplied CV PDF.

## Local preview
Serve this directory with any static HTTP server, for example:

```bash
python -m http.server 8080
```

Then open `http://localhost:8080/`.
